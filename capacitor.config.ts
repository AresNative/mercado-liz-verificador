import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "verificador",
  webDir: "dist",
  /* server: {
    cleartext: true, // Permitir HTTP (solo en desarrollo)
    url: "http://localhost:8100", // URL del servidor para desarrollo
  }, */
};

export default config;
